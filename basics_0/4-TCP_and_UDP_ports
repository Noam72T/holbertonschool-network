#!/usr/bin/env bash
# Display all listening TCP and UDP sockets with associated PID and program name

echo "Active Internet connections (only servers)"
ss -tulnp

echo "Active UNIX domain sockets (only servers)"
ss -xlnp
